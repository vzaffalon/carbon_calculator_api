import { Emissions } from "../interfaces/emissions.interface";
import emissions from "../store/emissions.store";

export const create = async (emissions: Emissions): Promise<Emissions[]> => {
    return []
}